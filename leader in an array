vector<int> superiorElements(vector<int>&a) {
    int n=a.size();
    vector<int> ans;
    int l=a[n-1];
    ans.push_back(l);
    for(int i=n-2;i>=0;i--){
        if(a[i]>l){
            ans.push_back(a[i]);
            l=a[i];
        }
        // l=max(l,a[i]);
    }
    sort(ans.begin(),ans.end());
    return ans;
}
